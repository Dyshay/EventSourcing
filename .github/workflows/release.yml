name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (semver format: x.y.z)'
        required: true
        type: string
      prerelease:
        description: 'Mark as pre-release'
        required: false
        type: boolean
        default: false
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'

    - name: Determine version
      id: get_version
      run: |
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          VERSION="${{ github.event.inputs.version }}"
        else
          VERSION="${GITHUB_REF#refs/tags/v}"
        fi
        echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
        echo "TAG=v$VERSION" >> $GITHUB_OUTPUT

    - name: Restore dependencies
      run: dotnet restore EventSourcing.sln

    - name: Build
      run: dotnet build EventSourcing.sln --configuration Release --no-restore
      
    - name: Test
      run: dotnet test EventSourcing.sln --configuration Release --no-build
      env:
        # Use secret if available, otherwise fall back to local MongoDB service
        MONGODB_CONNECTION_STRING: ${{ secrets.MONGODB_CONNECTION_STRING || 'mongodb://localhost:27017' }}
        CI: true
        
    - name: Create and push tag
      if: github.event_name == 'workflow_dispatch'
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git tag -a "${{ steps.get_version.outputs.TAG }}" -m "Release ${{ steps.get_version.outputs.VERSION }}"
        git push origin "${{ steps.get_version.outputs.TAG }}"

    - name: Pack NuGet packages
      run: |
        mkdir -p ./packages
        dotnet pack src/EventSourcing.Abstractions/EventSourcing.Abstractions.csproj \
          --configuration Release \
          --no-build \
          --output ./packages \
          -p:PackageVersion=${{ steps.get_version.outputs.VERSION }} \
          -p:IncludeSymbols=true \
          -p:SymbolPackageFormat=snupkg

        dotnet pack src/EventSourcing.Core/EventSourcing.Core.csproj \
          --configuration Release \
          --no-build \
          --output ./packages \
          -p:PackageVersion=${{ steps.get_version.outputs.VERSION }} \
          -p:IncludeSymbols=true \
          -p:SymbolPackageFormat=snupkg

        dotnet pack src/EventSourcing.MongoDB/EventSourcing.MongoDB.csproj \
          --configuration Release \
          --no-build \
          --output ./packages \
          -p:PackageVersion=${{ steps.get_version.outputs.VERSION }} \
          -p:IncludeSymbols=true \
          -p:SymbolPackageFormat=snupkg

        dotnet pack src/EventSourcing.CQRS/EventSourcing.CQRS.csproj \
          --configuration Release \
          --no-build \
          --output ./packages \
          -p:PackageVersion=${{ steps.get_version.outputs.VERSION }} \
          -p:IncludeSymbols=true \
          -p:SymbolPackageFormat=snupkg

    - name: List generated packages
      run: |
        echo "Generated packages:"
        ls -la ./packages/

    - name: Validate packages
      run: |
        echo "Validating NuGet packages..."
        for package in ./packages/*.nupkg; do
          echo "Checking $package"
          dotnet nuget verify "$package" || echo "Warning: Package verification failed for $package"
        done

    - name: Upload packages as artifacts
      uses: actions/upload-artifact@v4
      with:
        name: nuget-packages-${{ steps.get_version.outputs.VERSION }}
        path: ./packages/
        retention-days: 30

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ steps.get_version.outputs.TAG }}
        name: Release ${{ steps.get_version.outputs.VERSION }}
        files: |
          ./packages/*.nupkg
          ./packages/*.snupkg
        generate_release_notes: true
        prerelease: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.prerelease == 'true' || false }}
        body: |
          ## 📦 NuGet Packages (v${{ steps.get_version.outputs.VERSION }})

          **⚠️ Les packages sont disponibles en téléchargement ci-dessous et dans les artifacts pour upload manuel vers NuGet.**

          | Package | Description |
          |---------|-------------|
          | EventSourcing.Abstractions | Core abstractions and interfaces |
          | EventSourcing.Core | Core implementation with repository and snapshots |
          | EventSourcing.MongoDB | MongoDB provider for event and snapshot stores |
          | EventSourcing.CQRS | CQRS implementation with command/query buses |

          ### 📥 Upload Manuel vers NuGet

          1. Téléchargez les packages depuis cette release ou les artifacts
          2. Utilisez la commande suivante pour chaque package :
          ```bash
          dotnet nuget push EventSourcing.*.nupkg --api-key YOUR_API_KEY --source https://api.nuget.org/v3/index.json
          ```

          ### 🚀 Installation (une fois publié sur NuGet)

          **For MongoDB Event Sourcing:**
          ```bash
          dotnet add package EventSourcing.MongoDB --version ${{ steps.get_version.outputs.VERSION }}
          ```

          **For CQRS with Event Sourcing:**
          ```bash
          dotnet add package EventSourcing.CQRS --version ${{ steps.get_version.outputs.VERSION }}
          dotnet add package EventSourcing.MongoDB --version ${{ steps.get_version.outputs.VERSION }}
          ```

          **Core only:**
          ```bash
          dotnet add package EventSourcing.Core --version ${{ steps.get_version.outputs.VERSION }}
          ```

          ### 🔗 Links
          - 📚 [Documentation](https://github.com/Dyshay/EventSourcing#readme)
          - 🐛 [Report Issues](https://github.com/Dyshay/EventSourcing/issues)
          - 💬 [Discussions](https://github.com/Dyshay/EventSourcing/discussions)
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
